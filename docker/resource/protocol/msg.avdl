// This protocol is only designed for YOLO network object detection application.
// The return value is a array which should be [x min, y min, x max, y max, label, score]
protocol msg {
    array<bytes> forward(bytes input, array<int> input_shape, string input_type);
}
